<Window x:Class="ZoDream.Spider.Pages.ProxyView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="添加代理" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button x:Name="TestBtn" Width="80" Content="检测" Click="TestBtn_Click"/>
            <Button x:Name="ConfirmBtn" Width="80" Content="确定" Click="ConfirmBtn_Click"/>
        </StackPanel>
        <TextBox x:Name="ContentTb" Grid.Row="1" TextWrapping="Wrap" AcceptsReturn="True"/>
        <TextBlock Grid.Row="2"/>
    </Grid>
</Window>
