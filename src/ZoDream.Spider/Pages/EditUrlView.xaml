<Window x:Class="ZoDream.Spider.Pages.EditUrlView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ZoDream.Spider.Pages"
        mc:Ignorable="d"
        Title="添加网址" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="60"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="起始网址：" Style="{StaticResource CenterTb}"/>
            <TextBlock x:Name="AmountTb" Grid.Column="1" VerticalAlignment="Center"/>
            <Button x:Name="ConfirmBtn" Click="ConfirmBtn_Click" Content="确定" Grid.Column="2"/>
        </Grid>
        <TextBox x:Name="ContentTb" Grid.Row="1"
                 AcceptsReturn="True" TextChanged="ContentTb_TextChanged"/>
        
    </Grid>
</Window>
